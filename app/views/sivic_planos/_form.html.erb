<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title"><%= 'Cadastro de Planos'%></h3>
  </div>
  <div class="panel-footer">

    <%= form_for(@sivic_plano) do |f| %>
     <% if @sivic_plano.errors.any? %>
    
      <div class="alert alert-danger alert-dismissible fade in" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>

        <% if @sivic_plano.errors.any? %>
  
              <% @sivic_plano.errors.full_messages.each do |msg| %>

          <%= msg %></br>
        <% end %>
      <% end %>
      </div>
    <% end %>


<div class="row">
    <div class="col-xs-6">
      <%= f.label :nome_plano %><br>
      <%= f.text_field :nome_plano, :class =>  'form-control' %>
    </div>
     <div class="col-xs-3">
      <%= f.label :Qtd_Membros %><br>
      <%= f.number_field :numr_qtdmembro, :class =>  'form-control' %>
    </div>
     <div class="col-xs-3">
      <%= f.label :Qtd_Usuários %><br>
      <%= f.number_field :numr_qtdusuario, :class =>  'form-control' %>
    </div>

</div>
<br>
 </div>
</div>

<div class="btn-group">
  <button type="submit" class="btn btn-default btn">
    <span class="glyphicon glyphicon-floppy-disk"></span> Salvar
  </button>

  <%= yield(:iconexibir) %>

  <%= link_to sivic_planos_path, :class => "btn btn-default" do %>
    <span class="glyphicon glyphicon-arrow-left"></span> Voltar
  <% end %>
</div>



<% end %>



  </div>            
</div>
