<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title"><%= 'Cadastro de Escolaridades'%></h3>
  </div>
  <div class="panel-footer">

    <%= form_for(@sivic_escolaridade) do |f| %>
      <% if @sivic_escolaridade.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@sivic_escolaridade.errors.count, "error") %> prohibited this sivic_escolaridade from being saved:</h2>

          <ul>
          <% @sivic_escolaridade.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="row">
        <div class="col-xs-4">
        </div>      
        <div class="col-xs-4">
            <%= f.label :NOME_escolaridade, :class => 'lblForms'  %>
            <%= f.text_field :NOME_escolaridade, :class => 'form-control' %>
        </div>
      </div>
  </div>
</div>

<div class="btn-group">

  <button type="submit" class="btn btn-default btn">
    <span class="glyphicon glyphicon-floppy-disk"></span> Salvar
  </button>

  <%= yield(:iconexibir) %>

  <%= link_to sivic_escolaridades_path, :class => "btn btn-default" do %>
    <span class="glyphicon glyphicon-arrow-left"></span> Voltar
  <% end %>

</div>

    <% end %>
