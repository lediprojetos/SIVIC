<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title"><%= 'Cadastro de Ministérios'%></h3>
  </div>
  <div class="panel-footer">

<%= form_for(@sivic_ministerio) do |f| %>
  <% if @sivic_ministerio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sivic_ministerio.errors.count, "error") %> prohibited this sivic_ministerio from being saved:</h2>

      <ul>
      <% @sivic_ministerio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="col-xs-4">
  </div>
  <div class="col-xs-4">
    <%= f.label :Nome_Ministerio %>
    <%= f.text_field :nome_ministerio, :class => 'form-control' %>
    <%= f.label :sIGla %>
    <%= f.text_field :sigl_ministerio, :class => 'form-control' %>
  </div>

  <%= f.hidden_field :sivic_igreja_id, :value => current_user.sivic_pessoa.sivic_igreja_id %>

</div>
<br />
<br />
</div>
</div>


<div class="btn-group">

  <button type="submit" class="btn btn-default btn">
    <span class="glyphicon glyphicon-floppy-disk"></span> Salvar
  </button>

  <%= yield(:iconexibir) %>

  <%= link_to sivic_ministerios_path, :class => "btn btn-default" do %>
    <span class="glyphicon glyphicon-arrow-left"></span> Voltar
  <% end %>

</div>

<% end %>

