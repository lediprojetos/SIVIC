<%= form_for(@sivic_cidade) do |f| %>
  <% if @sivic_cidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sivic_cidade.errors.count, "error") %> prohibited this sivic_cidade from being saved:</h2>

      <ul>
      <% @sivic_cidade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :estado_id %><br>
    <%= f.number_field :estado_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
